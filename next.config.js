/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/SpeedFusion-pro',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
}

module.exports = nextConfig
